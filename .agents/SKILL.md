# Agent Instructions

## Project Structure

- **Ignore `target/` directory** — this is a Rust build output folder and should not be analyzed, searched, or modified. Only look at code in `src/`, `tests/`, and other source directories.
